#jinja2: lstrip_blocks: true
#
# {{ ansible_managed }}
#

{% for section, values in item.value.config.items() %}
[{{ section }}]
  {% for key, value in values.items() %}
    {% if value | length > 0 %}
    {{ key }} = {{ value }}
    {% endif %}
  {% endfor %}

{% endfor %}